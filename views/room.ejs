<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Chat</title>

    <script>
        const ROOM_ID = "<%= roomId %>"; // Injecting roomId from server
    </script>
    <script defer src="https://unpkg.com/peerjs@1.5.4/dist/peerjs.min.js"></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="script.js" defer></script> <!-- Ensure this path is correct -->
    
    <style>
        #video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            grid-auto-rows: 300px;
            gap: 10px;
        }
        
        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border: 2px solid #ccc; /* Optional: adds a border for better visibility */
        }
        
        .placeholder {
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f0f0f0;
            font-size: 1.5em;
            color: #999;
        }
    </style>
</head>
<body>
    <div id="video-grid"></div>

    <script>
        function addPlaceholder(userId) {
            const placeholder = document.createElement('div');
            placeholder.className = 'placeholder';
            placeholder.textContent = `User ${userId} has no camera`;
            document.getElementById('video-grid').append(placeholder);
        }
    </script>
</body>
</html>
